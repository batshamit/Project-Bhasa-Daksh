{
  "name": "project-bhasa-daksh",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "predeploy": "npm run build",
    "deploy": "npm run build && npx wrangler pages deploy frontend/out"
  }
}
